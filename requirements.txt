# Runtime dependencies, pinned so a run is reproducible.
pandas==3.0.5
SQLAlchemy==2.0.52
typer==0.27.1
rich==15.0.0

# Windows ships no IANA time zone database, and the pipeline converts naive
# New York wall-clock timestamps to UTC. Without this, zoneinfo raises there.
tzdata==2026.3
